{
  "name": "savon-scripts",
  "version": "1.0.0",
  "description": "Migration and utility scripts",
  "scripts": {
    "migrate": "ts-node migrate-campaigns-to-s3.ts",
    "backfill-gsi": "ts-node backfill-gsi-fields.ts",
    "backfill-gsi:dry": "DRY_RUN=true ts-node backfill-gsi-fields.ts",
    "backfill-jobs-pk": "ts-node backfill-jobs-pk.ts",
    "backfill-jobs-pk:dry": "DRY_RUN=true ts-node backfill-jobs-pk.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
